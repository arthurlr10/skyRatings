import React from "react";

import GlobalContainer from "@components/globalContainer";

const Home: React.FC = () => {
  return (
    <GlobalContainer>
      
    </GlobalContainer>
  );
};

export default Home;
